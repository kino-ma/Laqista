syntax = "proto3";

package face;

service Detector {
  rpc DetectFace(DetectRequest) returns (DetectReply) {}
}

message DetectRequest { repeated float data = 1; }
message DetectReply { repeated float squeezenet0_flatten0_reshape0 = 1; }